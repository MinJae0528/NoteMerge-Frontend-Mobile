import { View, Text } from "react-native";
import styled from "styled-components/native";

const Container = styled(View)``;
const Title = styled(Text)``;

const HomePresenter = () => {
  return (
    <Container>
      <Title>Home 메인화면입니다다</Title>
    </Container>
  );
};

export default HomePresenter;
